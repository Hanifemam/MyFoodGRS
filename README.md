# MyFoodGRS
MyFoodGRS data


## Files

- `actual_users_preferences.csv`: Actual preferences of real users.
- `prediction_of_actual_users_preferences_by_human.csv`: Predictions of human experts on actual users' preferences.
- `group_scores`: contains `user_id` (user identifier), `organizer_id` (group organizer identifier), `group_type` (user's group type), `rest_id`, and `rating`, where `rating` reflects each user's score for the restaurant based on whole-group preferences.
- `mean_individual_loss`: contains `user_id` (user identifier), `organizer_id` (group organizer identifier), `group_type` (user's group type), `rest_id`, and `rating`, where `rating` reflects each user's score for the restaurant based on that individual's own preferences.

